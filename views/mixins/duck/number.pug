mixin number(obj, field, title)
	.form-group(duck-field=field, duck-type='number')&attributes(attributes)
		if (title)
			label= title
				input.form-control(type='number', duck-value, value= obj && obj[field])
		else
			input.form-control(type='number', duck-value, value= obj && obj[field])